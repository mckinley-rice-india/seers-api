parameters:
  - name: pretty_print
    in: query
    description: Pretty print response
    schema:
      type: boolean
get:
  tags:
<<<<<<< HEAD:spec/paths/admin@{username}.yaml
    - Admin
  summary: Get admin user by user name
=======
    - User
  summary: Get user by user name
>>>>>>> parent of 7add5d4... New tags and functionality added:spec/paths/users@{username}.yaml
  description: |
    Some description of the operation. 
    You can use `markdown` here.
  operationId: getAdminByName
  parameters:
    - name: username
      in: path
      description: The name that needs to be fetched
      required: true
      schema:
        type: string
    - name: with_email
      in: query
      description: Filter users without email
      schema:
        type: boolean
  security:
    - main_auth:
        - 'read:users'
    - api_key: []
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/User'
          example:
            username: user1
            email: user@columbusx.com
    '403':
      description: Forbidden
    '404':
      description: User not found
put:
  tags:
<<<<<<< HEAD:spec/paths/admin@{username}.yaml
    - Admin
  summary: Add Admin  user
=======
    - User
  summary: Updated user
>>>>>>> parent of 7add5d4... New tags and functionality added:spec/paths/users@{username}.yaml
  description: This can only be done by the logged in user.
  operationId: addAdminUser
  parameters:
    - name: username
      in: path
      description: The name that needs to be updated
      required: true
      schema:
        type: string
  security:
    - main_auth:
        - 'write:users'
  responses:
    '200':
      description: OK
    '400':
      description: Invalid user supplied
    '404':
      description: User not found
  requestBody:
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/User'
      application/xml:
        schema:
          $ref: '#/components/schemas/User'
    description: Updated user object
    required: true
